node_modules/
coverage/
.next/
out/
.cache/
ldraw-library/
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Regenerated by `next dev`; the project keeps its own guidance in .claude/
AGENTS.md
/CLAUDE.md

# Regenerated by `pnpm test:coverage`; the audit reads it for CRAP scores.
coverage/
